# 商品库存与秒杀系统设计文档

## 一、系统架构草图

### 1.1 系统架构概述

本系统采用微服务架构，将业务拆分为四个核心服务：用户服务、商品服务、订单服务、库存服务。

```
─────────────────────────────────────────────────────────────────┐
│                         客户端层                                 │
│                    (Web / Mobile / API)                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         API 网关层                               │
│                   (Nginx / Spring Cloud Gateway)                │
│              - 路由转发  - 负载均衡  - 限流熔断                  │
└─────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────    ┌──────────────┐
│  用户服务     │    │  商品服务     │    │  订单服务     │
│  User        │    │  Product     │    │  Order       │
│  Service     │    │  Service     │    │  Service     │
│  :8081       │    │  :8082       │    │  :8083       │
└──────────────┘    └──────────────    └──────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              ▼
                    ┌──────────────
                    │  库存服务     │
                    │  Inventory   │
                    │  Service     │
                    │  :8084       │
                    └──────────────
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────    ┌──────────────┐
│   MySQL      │    │   Redis      │    │  RabbitMQ    │
│   数据库      │    │   缓存       │    │  消息队列     │
└──────────────┘    └──────────────┘    └──────────────┘
```

### 1.2 服务拆分说明

| 服务名称 | 端口 | 职责描述 |
|---------|------|---------|
| 用户服务 (user-service) | 8081 | 用户注册、登录、信息管理 |
| 商品服务 (product-service) | 8082 | 商品展示、详情查询、商品管理 |
| 订单服务 (order-service) | 8083 | 订单创建、查询、订单管理 |
| 库存服务 (inventory-service) | 8084 | 库存查询、扣减、库存管理 |

---

## 二、各服务 API 接口定义

### 2.1 用户服务 (User Service)

#### 2.1.1 用户注册
```
POST /api/v1/users/register
Content-Type: application/json

Request Body:
{
  "username": "string",
  "password": "string",
  "email": "string",
  "phone": "string"
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": "long",
    "username": "string"
  }
}
```

#### 2.1.2 用户登录
```
POST /api/v1/users/login
Content-Type: application/json

Request Body:
{
  "username": "string",
  "password": "string"
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": "long",
    "username": "string",
    "token": "string"
  }
}
```

#### 2.1.3 获取用户信息
```
GET /api/v1/users/{userId}
Authorization: Bearer {token}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": "long",
    "username": "string",
    "email": "string",
    "phone": "string"
  }
}
```

### 2.2 商品服务 (Product Service)

#### 2.2.1 获取商品列表
```
GET /api/v1/products?page={page}&size={size}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "products": [
      {
        "productId": "long",
        "productName": "string",
        "price": "decimal",
        "stock": "int",
        "imageUrl": "string"
      }
    ]
  }
}
```

#### 2.2.2 获取商品详情
```
GET /api/v1/products/{productId}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "productId": "long",
    "productName": "string",
    "description": "string",
    "price": "decimal",
    "stock": "int",
    "category": "string",
    "imageUrl": "string"
  }
}
```

#### 2.2.3 创建商品
```
POST /api/v1/products
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
  "productName": "string",
  "description": "string",
  "price": "decimal",
  "stock": "int",
  "category": "string"
}
```

### 2.3 订单服务 (Order Service)

#### 2.3.1 创建订单
```
POST /api/v1/orders
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
  "userId": "long",
  "productId": "long",
  "quantity": "int"
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "orderId": "string",
    "userId": "long",
    "productId": "long",
    "quantity": "int",
    "totalPrice": "decimal",
    "status": "string"
  }
}
```

#### 2.3.2 获取订单详情
```
GET /api/v1/orders/{orderId}
Authorization: Bearer {token}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "orderId": "string",
    "userId": "long",
    "productId": "long",
    "quantity": "int",
    "totalPrice": "decimal",
    "status": "string",
    "createTime": "datetime"
  }
}
```

#### 2.3.3 获取用户订单列表
```
GET /api/v1/orders/user/{userId}?page={page}&size={size}
Authorization: Bearer {token}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 50,
    "orders": [...]
  }
}
```

### 2.4 库存服务 (Inventory Service)

#### 2.4.1 查询库存
```
GET /api/v1/inventory/{productId}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "productId": "long",
    "stock": "int",
    "lockedStock": "int",
    "availableStock": "int"
  }
}
```

#### 2.4.2 扣减库存（秒杀核心接口）
```
POST /api/v1/inventory/deduct
Content-Type: application/json

Request Body:
{
  "productId": "long",
  "quantity": "int",
  "orderId": "string"
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "success": "boolean",
    "remainingStock": "int"
  }
}
```

#### 2.4.3 回滚库存
```
POST /api/v1/inventory/rollback
Content-Type: application/json

Request Body:
{
  "productId": "long",
  "quantity": "int",
  "orderId": "string"
}
```

---

## 三、数据库 ER 图

### 3.1 ER 图

```
┌─────────────────┐       ┌─────────────────┐
│    用户表        │       │    商品表        │
│   (users)       │       │   (products)    │
├─────────────────┤       ├─────────────────┤
│ PK userId       │       │ PK productId    │
│    username     │       │    productName  │
│    password     │       │    description  │
│    email        │       │    price        │
│    phone        │       │    categoryId   │
│    createTime   │       │    imageUrl     │
│    updateTime   │       │    createTime   │
─────────────────┘       │    updateTime   │
         │                └─────────────────┘
         │                         │
         │                ┌─────────────────┐
         │                │    库存表        │
         │                │  (inventory)    │
         │                ├─────────────────┤
         │                │ PK inventoryId  │
         │                │ FK productId    │
         │                │    stock        │
         │                │    lockedStock  │
         │                │    createTime   │
         │                │    updateTime   │
         │                └─────────────────┘
         │
─────────────────┐
│    订单表        │
│   (orders)      │
├─────────────────┤
│ PK orderId      │
│ FK userId       │
│ FK productId    │
│    quantity     │
│    totalPrice   │
│    status       │
│    createTime   │
│    updateTime   │
└─────────────────┘
```

### 3.2 数据表详细设计

#### 3.2.1 用户表 (users)
```sql
CREATE TABLE users (
    userId BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户 ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(255) NOT NULL COMMENT '密码 (加密)',
    email VARCHAR(100) COMMENT '邮箱',
    phone VARCHAR(20) COMMENT '手机号',
    createTime DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_username (username),
    INDEX idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 3.2.2 商品表 (products)
```sql
CREATE TABLE products (
    productId BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '商品 ID',
    productName VARCHAR(200) NOT NULL COMMENT '商品名称',
    description TEXT COMMENT '商品描述',
    price DECIMAL(10, 2) NOT NULL COMMENT '价格',
    categoryId INT COMMENT '分类 ID',
    imageUrl VARCHAR(500) COMMENT '商品图片 URL',
    createTime DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_category (categoryId),
    INDEX idx_price (price)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

#### 3.2.3 库存表 (inventory)
```sql
CREATE TABLE inventory (
    inventoryId BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '库存 ID',
    productId BIGINT NOT NULL UNIQUE COMMENT '商品 ID',
    stock INT NOT NULL DEFAULT 0 COMMENT '总库存',
    lockedStock INT NOT NULL DEFAULT 0 COMMENT '锁定库存',
    createTime DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_product (productId)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='库存表';
```

#### 3.2.4 订单表 (orders)
```sql
CREATE TABLE orders (
    orderId VARCHAR(64) PRIMARY KEY COMMENT '订单 ID',
    userId BIGINT NOT NULL COMMENT '用户 ID',
    productId BIGINT NOT NULL COMMENT '商品 ID',
    quantity INT NOT NULL COMMENT '购买数量',
    totalPrice DECIMAL(10, 2) NOT NULL COMMENT '订单总价',
    status TINYINT NOT NULL DEFAULT 0 COMMENT '订单状态：0-待支付 1-已支付 2-已发货 3-已完成 4-已取消',
    createTime DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updateTime DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_user (userId),
    INDEX idx_product (productId),
    INDEX idx_status (status),
    INDEX idx_create_time (createTime)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

---

## 四、技术栈选型说明

### 4.1 技术栈总览

| 层次 | 技术选型 | 说明 |
|------|---------|------|
| **编程语言** | Java 17 | LTS 版本，性能优异，生态成熟 |
| **后端框架** | Spring Boot 3.x | 快速开发，约定优于配置 |
| **ORM 框架** | MyBatis-Plus | 简化 SQL 操作，支持动态 SQL |
| **数据库** | MySQL 8.0 | 成熟稳定的关系型数据库 |
| **缓存** | Redis 7.x | 高性能缓存，支持分布式锁 |
| **消息队列** | RabbitMQ | 异步解耦，流量削峰 |
| **服务治理** | Spring Cloud Alibaba | 服务注册发现、配置中心 |
| **API 网关** | Spring Cloud Gateway | 统一入口，限流熔断 |
| **权限认证** | Spring Security + JWT | 无状态认证授权 |
| **接口文档** | Swagger/OpenAPI | 自动生成 API 文档 |

### 4.2 选型理由

#### 4.2.1 Spring Boot + MyBatis
- **开发效率高**：自动配置，快速启动
- **生态完善**：丰富的 starter 依赖
- **性能优异**：MyBatis 灵活控制 SQL，适合复杂查询

#### 4.2.2 MySQL
- **成熟稳定**：经过多年验证的关系型数据库
- **事务支持**：ACID 事务保证数据一致性
- **索引优化**：支持多种索引类型，查询效率高

#### 4.2.3 Redis
- **高性能**：内存存储，读写速度极快
- **丰富数据结构**：String、Hash、List、Set、ZSet
- **分布式锁**：实现秒杀场景下的库存扣减原子性

#### 4.2.4 RabbitMQ
- **异步解耦**：订单创建与库存扣减异步处理
- **流量削峰**：缓解秒杀时的高并发压力
- **可靠投递**：保证消息不丢失

### 4.3 项目依赖管理 (pom.xml 核心依赖)

```xml
<dependencies>
    <!-- Spring Boot Starter -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
        <version>3.2.0</version>
    </dependency>
    
    <!-- MyBatis-Plus -->
    <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        <version>3.5.5</version>
    </dependency>
    
    <!-- MySQL Driver -->
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <version>8.2.0</version>
    </dependency>
    
    <!-- Redis -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-redis</artifactId>
        <version>3.2.0</version>
    </dependency>
    
    <!-- RabbitMQ -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-amqp</artifactId>
        <version>3.2.0</version>
    </dependency>
    
    <!-- JWT -->
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>0.12.3</version>
    </dependency>
    
    <!-- Lombok -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>1.18.30</version>
    </dependency>
</dependencies>
```

---

## 五、秒杀场景特殊设计

### 5.1 秒杀流程

```
用户请求 → API 网关限流 → Redis 预减库存 → 消息队列异步下单 → 数据库最终扣减
```

### 5.2 核心技术点

1. **Redis 预减库存**：活动开始前将库存加载到 Redis，请求时直接在 Redis 中扣减
2. **RabbitMQ 异步下单**：库存扣减成功后发送消息到 MQ，异步创建订单
3. **分布式锁**：使用 Redis Lua 脚本保证库存扣减的原子性
4. **限流熔断**：网关层对同一用户/IP 限流，防止刷单
5. **防超卖**：数据库乐观锁 + 唯一索引保证数据一致性
